{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api", "dest": "/api" },
    { "src": "/api/(.*)", "dest": "/api" }
  ],
  "installCommand": "npm install && cd backend && npm install"
}
